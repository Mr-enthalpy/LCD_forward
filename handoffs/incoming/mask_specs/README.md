# Incoming Mask Specs

Placeholder only.

This directory is for documentation or tiny examples describing `lcd_mask_families` mask identity inputs such as `MaskInstanceSpec`, `MaskSequenceSpec`, family metadata, projection policy, and deterministic hashes.

Rules:

- Do not commit large data to Git.
- Do not define final `lcd_mask_families` schemas here.
- Do not add external repository imports.
- Store real artifacts in ignored outputs or third-party storage.
- Commit only docs, manifests, or tiny examples unless explicitly scoped otherwise.
