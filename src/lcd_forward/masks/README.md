# Masks

Placeholder only.

This directory reserves the future mainline architecture slot for local wrappers around `lcd_mask_families` mask identity.

No implementation exists yet. Do not add external repository imports, final schema commitments, mask-family renderers, projection policies, hashes, or core family definitions here.
