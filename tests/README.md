# Tests

The active test suite is intentionally minimal while the repository only contains the placeholder mainline package shape.

Do not add operator, loader, surrogate, reconstruction, OTF, or H-matrix tests before those components are explicitly implemented.
