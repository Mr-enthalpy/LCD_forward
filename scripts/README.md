# Scripts

Placeholder only.

Executable mainline scripts will be introduced only after measured-evidence contracts and operator prototypes exist.

The legacy dense forward/reconstruction training and evaluation scripts were removed from the active tree. They are preserved in Git history and described in `docs/legacy_thesis_prototype.md`.

Do not add new executable scripts here until the corresponding mainline contract or prototype is explicitly scoped.
