seed: 42

forward_model_name: complex_field_basis

num_wavelengths: 3
num_frames: 4

mask_size: 64
image_size: 64
psf_size: 17

forward_rank: 24
forward_embed_dim: 128
normalize_psf: true
coeff_scale: 1.0

recon_hidden: 64
nonnegative_output: false

batch_size: 4
epochs: 20
learning_rate: 1.0e-3
weight_decay: 0.0
num_workers: 0

spectral_response: [1.0, 1.0, 1.0]
noise_std: 0.0

loss:
  w_l1: 1.0
  w_mse: 0.5
  w_tv: 0.0

forward_checkpoint: outputs/forward/best_forward.pt
recon_checkpoint: null